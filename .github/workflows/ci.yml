name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linear-history:
    name: Linear PR history
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Check out complete pull-request history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Reject merge commits introduced by the pull request
        shell: pwsh
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: ./scripts/check-linear-history.ps1 -BaseSha $env:BASE_SHA -HeadSha $env:HEAD_SHA

  quality:
    name: Types, tests, harness, and builds
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run strict types and renderer-free tests
        run: bun run check

      - name: Run deterministic full-scene harness
        run: bun run harness
        env:
          CAPE_ENFORCE_PERFORMANCE_BUDGET: 'false'

      - name: Build production bundle
        run: bun run build

      - name: Build GitHub Pages bundle
        run: bun run build:pages

      - name: Verify Pages-safe asset paths
        run: bun run check:pages

  # Branch protection retains this legacy context name. CI runs only a short,
  # deterministic renderer smoke; the 37-view audit remains opt-in locally.
  render-audit:
    name: Dynamic multi-angle render audit
    if: github.event_name == 'pull_request'
    runs-on: windows-2022
    timeout-minutes: 2
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Exercise the default WebGL renderer
        run: bun run profile:render
        env:
          CAPE_PROFILE_RENDERER: webgl
          CAPE_PROFILE_DURATION_SECONDS: '0.05555555555555555'
          CAPE_PROFILE_SETTLE_SECONDS: '0.05'
          CAPE_PROFILE_RUNNING_WARMUP_SECONDS: '0.05'
          CAPE_PROFILE_SYNC_INTERVAL: '8'
          CAPE_PROFILE_OUTPUT: artifacts/performance/webgl-ci-smoke.json

      - name: Upload WebGL smoke diagnostics
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cape-physics-webgl-smoke
          path: artifacts/performance/webgl-ci-smoke.json
          if-no-files-found: error
          retention-days: 7
